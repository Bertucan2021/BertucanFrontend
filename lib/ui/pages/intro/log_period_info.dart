import 'package:flutter/material.dart';

class LogPeriodInfoPage extends StatelessWidget {
  const LogPeriodInfoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hello World'),
      ),
    );
  }
}
