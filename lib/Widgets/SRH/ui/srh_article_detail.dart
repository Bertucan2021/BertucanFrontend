import 'package:flutter/material.dart';

class SRHArticleDetailWidget extends StatelessWidget {
  const SRHArticleDetailWidget({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
